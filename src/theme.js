import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#CE5041",
    },
    secondary: {
      main: "#365862",
    },
    error: {
      main: "#676464",
    },
  },
});

export default theme;
