import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#CE5041",
      secondary: "#fff",
    },
    secondary: {
      main: "#365862",
      secondary: "#fff",
    },
    error: {
      main: "#676464",
    },
  },
});

export default theme;
